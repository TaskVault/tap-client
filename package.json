{
  "name": "tap",
  "description": "The Ad Platform",
  "keywords": [
    "blockchain",
    "e-commerce"
  ],
  "version": "0.1.0",
  "private": true,
  "repository": {
    "url": "https://github.com/TonAds/tonads",
    "type": "git"
  },
  "scripts": {
    "start": "nx run-many --parallel --maxParallel=50 -t start",
    "dev": "nx run-many --parallel --maxParallel=50 -t dev",
    "build": "nx run-many -t build",
    "clean": "nx run-many -t clean",
    "lint:fix": "nx run-many -t lint:fix",
    "prettify": "nx run-many -t prettify",
    "test": "nx run-many -t test"
  },
  "packageManager": "pnpm@9.1.4",
  "engines": {
    "node": "20.11.0",
    "pnpm": "9.1.4"
  },
  "devDependencies": {
    "nx": "^19.1.1"
  }
}
